'use client';
import { Box, Typography } from '@mui/material';

const MapComponent = () => {
  return (
    <Box sx={{
      height: 300,
      backgroundColor: '#e0e0e0',
      borderRadius: 2,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      mb: 3,
    }}>
      <Typography variant="h6">[Map Simulation Coming Soon]</Typography>
    </Box>
  );
};

export default MapComponent;
